# Supabase — copiá este archivo a .env.local y completá con tus valores reales.
# Los encontrás en el dashboard de Supabase: Settings → API.
# La anon key es pública (puede ir en el frontend); la seguridad la da RLS.
# NUNCA pongas la service_role key acá.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
